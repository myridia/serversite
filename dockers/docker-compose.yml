version: '3'
services:
  serversite_nginx:
    container_name: serversite_nginx
    image: nginx
    ports:
        - "80:80" 
       # - "443:443" 
    volumes:
      - ../public:/usr/share/nginx/html/
      - ./etc/nginx/conf.d:/etc/nginx/conf.d
    tty: true
